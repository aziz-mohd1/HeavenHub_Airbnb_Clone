<% layout("/layouts/boilerplate.ejs") %>

<body class="bg-gray-100 h-screen w-full flex items-center justify-center">
    <!-- Wrapper -->
    <div class="text-center flex flex-col items-center justify-center space-y-4 animate-fade-in mt-40">
        <!-- Title -->
        <h1 class="text-5xl font-extrabold text-black drop-shadow-md transition-transform duration-500 hover:scale-105">
            🚀 Heaven<span class="text-[#FF385C]">Hub.</span>  🚀
        </h1>

        <!-- Subtitle -->
        <p class="mt-2 text-gray-700 text-lg opacity-90">
            My Airbnb FullStack Clone - Major Project is live now 🎉
        </p>

        <!-- Button -->
        <a href="/listings"
           class="mt-6 inline-block px-6 py-3 bg-[#FF385C] text-white rounded-lg shadow-lg 
                  transition-all duration-300 hover:bg-[#e3304f] hover:scale-105">
           View Listings
        </a>
    </div>

    <!-- Loading Animation (appears briefly on page load) -->
    <div id="loading" class="fixed inset-0 flex items-center justify-center bg-white z-50">
        <div class="animate-spin rounded-full h-16 w-16 border-t-4 border-[#FF385C] border-solid"></div>
    </div>

    <!-- Small JS for loader -->
    <script>
        window.addEventListener("load", () => {
            const loader = document.getElementById("loading");
            loader.style.opacity = "0";
            setTimeout(() => loader.style.display = "none", 1500);
        });
    </script>
</body>
